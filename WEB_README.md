# Blueprint Editor - Web UI

基于 Vue 3 + TypeScript + Vue Flow 的蓝图可视化编辑器前端。

## 功能特性

- ✅ **可视化编辑** - 使用 Vue Flow 进行拖拽式节点编辑
- ✅ **节点调色板** - 丰富的内置节点类型库
- ✅ **实时执行** - 与后端 API 集成，实时执行蓝图
- ✅ **调试面板** - 实时监控节点执行状态和输出
- ✅ **导入/导出** - 支持 JSON 格式导入导出
- ✅ **深色主题** - 专业的深色 UI 设计

## 快速开始

### 1. 安装依赖

```bash
cd web
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

### 3. 启动后端 API 服务器

在另一个终端中：

```bash
go run cmd/server/main.go
```

### 4. 打开浏览器

访问 http://localhost:5173

## 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **TypeScript** - 类型安全
- **Vue Flow** - 流程图可视化库
- **Pinia** - 状态管理
- **Axios** - HTTP 客户端
- **Vite** - 快速构建工具

## 使用说明

### 创建节点

1. 从左侧节点调色板拖拽或点击节点
2. 节点会自动添加到画布中心

### 连接节点

1. 拖拽节点右侧的输出引脚（圆点）
2. 连接到另一个节点左侧的输入引脚

### 执行蓝图

1. 点击顶部工具栏的「执行」按钮
2. 查看右侧调试面板的执行结果
3. 节点会高亮显示执行状态

### 导入/导出

- **导出**: 点击「导出」按钮，保存为 JSON 文件
- **导入**: 点击「导入」按钮，选择 JSON 文件
